<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme -->
    <style name="Base.Theme.Chatz" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Customize your theme here -->
        <item name="materialButtonStyle">@style/Widget.MaterialComponents.Button</item>
        <item name="textInputStyle">@style/Widget.MaterialComponents.TextInputLayout.OutlinedBox</item>
    </style>

    <style name="Theme.Chatz" parent="Base.Theme.Chatz" />

    <!-- Splash Screen Theme -->
    <style name="App.Starting.Theme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Android 12+ Splash Screen API attributes -->
        <item name="windowSplashScreenBackground">#000000</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/splash_dark</item>

        <!-- Pre-Android 12 Splash Screen attributes -->
        <item name="android:windowSplashScreenBackground" tools:targetApi="31">#000000</item>
        <item name="android:windowSplashScreenAnimatedIcon" tools:targetApi="31">@drawable/splash_dark</item>

        <!-- Specify the theme to use after splash screen -->
        <item name="postSplashScreenTheme">@style/Theme.Chatz</item>
    </style>
</resources>